# macOS
.DS_Store
**/.DS_Store

# Node tooling for the image pipeline (devDependencies — see package.json)
node_modules/

# Local dev / verification artifacts (not shipped)
.tmp/
verify.mjs
probe*.mjs
